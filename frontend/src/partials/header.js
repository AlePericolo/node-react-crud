import React from "react";

const Header = () => {
    return (
        <header>
            <div className="container-fluid text-center py-2">
                Header
            </div>
        </header>
    );
};

export default Header;
